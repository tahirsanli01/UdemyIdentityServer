 AuthServer ->  https://localhost:5001
 Client1 => https://localhost:5006
 Client2 => https://localhost:5011;
  API1 => https://localhost:5016;
  API2 => https://localhost:5021;



    <!DOCTYPE html>
<html>
<head>
<style>
    body {
        font-family: Arial, sans-serif;
    }

    .chart-container {
        position: relative;
        width: 360px;
        height: 360px;
        margin: 50px auto;
    }

    .label {
        position: absolute;
        background: white;
        padding: 6px 10px;
        border-radius: 6px;
        border: 1px solid #ccc;
        font-size: 14px;
        transform: translate(-50%, -50%);
        white-space: nowrap;
    }
</style>
</head>
<body>

<div class="chart-container">
    <svg width="360" height="360" viewBox="0 0 42 42" id="chart" style="transform: rotate(-90deg);">
    </svg>
</div>

<script>
const values = [
    { value: 40, color: "#ff6b6b", label: "40 Puan" },
    { value: 25, color: "#4dabf7", label: "25 Puan" },
    { value: 20, color: "#51cf66", label: "20 Puan" },
    { value: 15, color: "#ffd43b", label: "15 Puan" }
];

const svg = document.getElementById("chart");

let cumulative = 0;

// Donut arka planı
svg.innerHTML += `
    <circle r="15.915" cx="21" cy="21" fill="transparent" stroke="#eee" stroke-width="6"></circle>
`;

values.forEach(v => {
    const dash = v.value;
    const gap = 100 - dash;
    const offset = -cumulative;

    // Dilimi çiz
    svg.innerHTML += `
        <circle 
            r="15.915" cx="21" cy="21"
            fill="transparent"
            stroke="${v.color}"
            stroke-width="6"
            stroke-dasharray="${dash} ${gap}"
            stroke-dashoffset="${offset}"
        ></circle>
    `;

    // Açıyı hesapla (dilim ortası)
    const angle = (cumulative + v.value / 2) * 3.6;
    const rad = (angle - 90) * (Math.PI / 180);

    // Etiketi donut'ın TAM dışına koy (130 → 160 px yapıldı)
    const distance = 160; // <<< HEMEN DIŞARIDA OLMASI İÇİN ARTTIRILDI

    const cx = 180 + Math.cos(rad) * distance;
    const cy = 180 + Math.sin(rad) * distance;

    const div = document.createElement("div");
    div.className = "label";
    div.style.left = cx + "px";
    div.style.top = cy + "px";
    div.innerText = v.label;

    document.querySelector(".chart-container").appendChild(div);

    cumulative += v.value;
});
</script>

</body>
</html>